      - name: Run agent registration script
        env:
          TOKU_API_KEY: ${{ secrets.TOKU_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          # ... include your other agent keys here ...
        run: python agent.py
